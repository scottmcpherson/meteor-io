# IF you use the io command to run your app
# these environment variables will automatically be
# imported into process.evn in your local environment.

# Example:
# MAIL_URL="smtp://username:password@smtp-url:port"
